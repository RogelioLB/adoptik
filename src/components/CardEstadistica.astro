---
interface Props{
    stat:number,
    text: string,
    percentage: number
}
const {stat,text,percentage} = Astro.props;
---

<div class="bg-gray-200 rounded-sm p-2">
  <h2 class="font-bold">{stat}</h2>
  <p>{text}</p>
  <p>{percentage}% <span class="text-gray-500">7d</span></p>
</div>
